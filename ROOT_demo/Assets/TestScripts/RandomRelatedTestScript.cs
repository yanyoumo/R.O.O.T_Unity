using System.Collections;
using System.Collections.Generic;
using System.Linq;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;
using ROOT;

namespace Tests
{
    public class RandomRelatedTestScript
    {
        /*// A Test behaves as an ordinary method
        [Test]
        public void SumZeroRandomArraySimplePasses()
        {
            var count = Random.Range(10, 30);
            for (int i = 0; i < count; i++)
            {
                var res=Utils.SumZeroRandomArray(Random.Range(1, 100), Random.Range(1, 100));
                Utils.DebugLogArray(res);
            }
        }

        [Test]
        public void SpreadOutLayingWRandomizationSimplePasses()
        {
            var count = Random.Range(10, 30);
            for (int i = 0; i < count; i++)
            {
                var length = Random.Range(1, 100);
                var res = Utils.SpreadOutLayingWRandomization(length, Random.Range(length, 100 * length),
                    Random.Range(0.0f, 1.0f));
                Utils.DebugLogArray(res);
            }
        }*/
    }
}
